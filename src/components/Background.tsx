

const Background = () => {
  return (
    <div 
      className="fixed top-0 left-0 w-full h-full bg-cover bg-left -z-10"
      style={{ backgroundImage: 'url(../image/background3.jpg)' }}
    />
  );
};

export default Background;